<section class="grid">
  <slot />
</section>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    margin-left: auto;
    margin-right: auto;
    margin-top: 2rem;
    justify-items: center;
  }
</style>
